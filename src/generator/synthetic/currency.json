{
  "name": "Currency",
  "istable": 0,
  "is_submittable": 0,
  "autoname": "field:currency_name",
  "_note": "Synthetic. Currency lives in the Frappe framework, not the erpnext app. Minimal master we seed ourselves.",
  "fields": [
    { "fieldname": "currency_name", "fieldtype": "Data", "reqd": 1 },
    { "fieldname": "symbol", "fieldtype": "Data" },
    { "fieldname": "enabled", "fieldtype": "Check", "default": "0" },
    { "fieldname": "fraction", "fieldtype": "Data" },
    { "fieldname": "fraction_units", "fieldtype": "Int" },
    { "fieldname": "smallest_currency_fraction_value", "fieldtype": "Currency" },
    { "fieldname": "number_format", "fieldtype": "Data" }
  ]
}
