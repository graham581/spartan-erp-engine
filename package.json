{
  "name": "spartan-erp-engine",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "Lean TypeScript business engine on Vercel + Supabase. Schema and document lifecycle generated from ERPNext DocType JSON (source of truth). Job-spine engine that will replace Ascora.",
  "scripts": {
    "generate": "tsx src/generator/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10"
  }
}
