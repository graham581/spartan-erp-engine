{
  "// note": "Opt-in type-checker ONLY (noEmit). Never compiles output — the engine runs plain JS. Turn on for keystone modules (perms, workflow) via `npm run check`.",
  "compilerOptions": {
    "checkJs": true,
    "allowJs": true,
    "noEmit": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "es2022",
    "strict": false,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.js"],
  "exclude": ["node_modules"]
}
